# .env (raíz del repo) — solo para docker-compose.yml
# Copiar como .env y ajustar si querés. Estas credenciales son las del
# contenedor de Postgres local, no confundir con backend/.env.
POSTGRES_DB=medicbolivia
POSTGRES_USER=medicbolivia
POSTGRES_PASSWORD=medicbolivia_dev_password
